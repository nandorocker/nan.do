---
import { Image } from "astro:assets";

export interface Props {
  title: string;
  category: string;
  description: string;
  imageSrc: string;
  imageAlt: string;
  linkUrl: string;
  linkText?: string;
  linkDisabled?: boolean;
  imageWidth?: number;
  imageHeight?: number;
}

const {
  title,
  category,
  description,
  imageSrc,
  imageAlt,
  linkUrl,
  linkText = "Read The Story",
  linkDisabled = false,
  imageWidth = 800,
  imageHeight = 600,
} = Astro.props;
---

<article class="flex flex-col gap-4 sm:gap-8">
  <Image
    src={imageSrc}
    alt={imageAlt}
    class="w-full h-52 sm:h-40 object-cover rounded-lg shadow-obsidian shadow-2xl-flat-r"
    width={imageWidth}
    height={imageHeight}
  />
  <header class="space-y-1 sm:space-y-2 mt-4 md:mt-0">
    <span class="text-sm uppercase font-bold">{category}</span>
    <h3 class="text-2xl md:text-2xl lg:text-3xl font-bold leading-tight mb-3">{title}</h3>
  </header>
  <p class="flex-grow mb-0">
    {description}
  </p>
  <a
    href={linkUrl}
    class:list={[
      "text-2xl underline decoration-2 hover:decoration-lavender transition-colors duration-300 inline-block",
      { disable: linkDisabled },
    ]}
  >
    {linkText}
  </a>
</article>
