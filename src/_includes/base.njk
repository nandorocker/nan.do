<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="{{ '/tailwind.css' | url }}">
    <link rel="stylesheet" type="text/css" href="{{ '/assets/css/style.css' | url }}">
</head>

<body>
    <main class="relative min-h-screen bg-lavender text-obsidian transition-colors duration-700 overflow-hidden"
        data-bg-scroll>
        {% include 'nav.njk' %}

        {{ content | safe }}

        {% include 'footer.njk' %}

    </main>
</body>

</html>